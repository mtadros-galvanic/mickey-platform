all:
  children:
    proxmox_hosts:
      hosts:
        mickey-pve:
          ansible_host: 10.25.1.207
          ansible_user: root
          ansible_ssh_private_key_file: /home/mtadros/.ssh/mickey
